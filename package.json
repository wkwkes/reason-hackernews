{
  "name": "reason-hackernews",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "bs-fetch": "^0.2.0",
    "bs-json": "^0.2.4",
    "gh-pages": "^1.1.0",
    "jest-cli": "^21.2.1",
    "react": "^16.1.1",
    "react-dom": "^16.1.1",
    "reason-scripts": "0.7.0"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "prepare": "npm link bs-platform"
  },
  "devDependencies": {
    "bs-jest": "^0.2.0",
    "reason-react": "^0.2.4"
  },
  "homepage": "https://wkwkes.github.io/reason-hackernews"
}
